name: Deploy to server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4

      - name: Deploy files
        run: |
          rsync -avz --delete \
            --exclude='.git*' \
            --exclude='_work' \
            $GITHUB_WORKSPACE/ /data01/virt53324/domeenid/www.jungent.eu/jng/
